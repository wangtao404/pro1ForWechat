HashMap的底层实现：
首先，HashMap在1.7之前是通过数组+链表的形式实现。在将一个数据put进Hash Map中的时候，会先对key求hash code，然后找到对应的位置，如果该位置上有数据，则比对key和hashcode,如果相同,那么就会覆盖,如果不同,就将该数据以链表的形式挂载到当前数据的后边.get数据的时候依然是通过key的hashCOde查找.
在1.8之后对hashcode的实现做了优化,在链表长度大于8的时候,不再使用链表,而是转为红黑树结构.可以提高查询效率.如果长度小于6,会转为链表.
古德古德.
